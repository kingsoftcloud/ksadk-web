import type { Config } from 'tailwindcss';

/** Keep utility selectors inside the consumer-owned conversation boundary. */
const config: Config = {
  important: '.ksadk-web',
};

export default config;
